const audioPlayer=document.querySelector(".audio-player");console.log(audioPlayer.getAttribute("datafile"));const audio=new Audio(audioPlayer.getAttribute("datafile"));audio.addEventListener("loadeddata",()=>{audioPlayer.querySelector(".time .length").textContent=getTimeCodeFromNum(audio.duration),audio.volume=.75},!1);const timeline=audioPlayer.querySelector(".progress");timeline.addEventListener("click",e=>{var t=e.pageX,a=timeline.getBoundingClientRect().left+window.scrollX,e=timeline.offsetWidth,a=t-a,e=audio.duration/e;audio.currentTime=e*a},!1),setInterval(()=>{const e=audioPlayer.querySelector(".timeline");var t=audio.currentTime/audio.duration*100+"%";e.style.background=`linear-gradient(to right, #fff ${t}, transparent 0)`,audioPlayer.querySelector(".time .current").textContent=getTimeCodeFromNum(audio.currentTime)},500);const playBtn=audioPlayer.querySelector(".controls .toggle-play");function getTimeCodeFromNum(e){var t=parseInt(e),a=parseInt(t/60);t-=60*a;e=parseInt(a/60);return a-=60*e,0===e?`${a}:${String(t%60).padStart(2,0)}`:`${String(e).padStart(2,0)}:${a}:${String(t%60).padStart(2,0)}`}playBtn.addEventListener("click",()=>{audio.paused?(playBtn.classList.remove("play"),playBtn.classList.add("pause"),audio.play()):(playBtn.classList.remove("pause"),playBtn.classList.add("play"),audio.pause())},!1);